# Turn welcome message off
startup_message off

# Status bar
caption always '%{= dg} %H %{G}| %{B}%l %{G}|%=%?%{d}%-w%?%{r}(%{d}%n %t%? {%u} %?%{r})%{d}%?%+w%?%=%{G}| %{B}%M %d %c:%s '

# Change default escape command
escape ^Oo

shelltitle "$ |idle"
