notARepository: 'skip'
promptToReturnFromSubprocess: false
os:
  open: '$HOME/.local/bin/open-in-nvim {{filename}}'
gui:
  nerdFontsVersion: "3"
  switchTabsWithPanelJumpKeys: true
  showNumstatInFilesView: true
  spinner:
    rate: 5000
keybinding:
  files:
    viewResetOptions: 'G'
  commits:
    viewResetOptions: 'G'
git:
  fetchAll: false
  pagers:
    - colorArg: always
      pager: delta --dark --paging=never
customCommands:
  - key: 'p'
    context: 'global'
    description: 'Pull current branch only'
    command: 'git pull {{.CheckedOutBranch.UpstreamRemote}} {{.CheckedOutBranch.UpstreamBranch}}'
    loadingText: 'Pulling current branch...'
  - key: 'D'
    context: 'files'
    description: 'diffmap — structural overview of uncommitted changes'
    command: 'diffmap | less -R'
    output: terminal
  - key: 'D'
    context: 'commits'
    description: 'diffmap — structural overview of the selected commit'
    command: 'diffmap {{.SelectedCommit.Hash}} | less -R'
    output: terminal
